name: Build Unsigned IPA

on:
  push:
    branches: [ main, master ]
    paths-ignore:
      - "ipa/**"
      - "README.md"
      - "**/*.md"
  workflow_dispatch:

concurrency:
  group: ios-build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build Unsigned IPA
    runs-on: macos-15
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          if [ -d "/Applications/Xcode_16.4.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer
          elif [ -d "/Applications/Xcode_16.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16.app/Contents/Developer
          elif [ -d "/Applications/Xcode.app" ]; then
            sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          else
            echo "Using default Xcode"
          fi
          xcode-select -p
          xcodebuild -version
          xcodebuild -showsdks || true

      - name: Archive unsigned
        run: |
          set -euo pipefail
          PROJECT_OR_WORKSPACE="-project Meine.xcodeproj"
          SCHEME="Meine"
          SIGNING="CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY= DEVELOPMENT_TEAM= CODE_SIGN_ENTITLEMENTS= EXPANDED_CODE_SIGN_IDENTITY="
          EXTRA="ENABLE_PREVIEWS=NO ENABLE_ON_DEMAND_RESOURCES=NO ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOLS=NO ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS=NO ASSETCATALOG_COMPILER_APPICON_NAME= ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME= SUPPORTED_PLATFORMS=iphoneos IPHONEOS_DEPLOYMENT_TARGET=17.0 COMPILER_INDEX_STORE_ENABLE=NO"

          xcodebuild -project Meine.xcodeproj -scheme "$SCHEME" -showdestinations || true
          xcodebuild archive \
            $PROJECT_OR_WORKSPACE \
            -scheme "$SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$RUNNER_TEMP/App.xcarchive" \
            -sdk iphoneos \
            $SIGNING \
            $EXTRA

      - name: Package unsigned IPA
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/export/Payload"
          cp -R "$RUNNER_TEMP/App.xcarchive/Products/Applications/"*.app "$RUNNER_TEMP/export/Payload/"
          cd "$RUNNER_TEMP/export"
          zip -qry App-unsigned.ipa Payload
          ls -lh App-unsigned.ipa
          unzip -l App-unsigned.ipa | head

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: unsigned-ipa
          path: ${{ runner.temp }}/export/*.ipa
          if-no-files-found: error
          retention-days: 14
